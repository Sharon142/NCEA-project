/* navigation screen opens when someone clicks "open" on top left corner */
function openNav() {
    document.getElementById("myNav").style.width = "100%";
  }
  
  /* Navigation screen closes when someone clicks "x" on the top right corner */
  function closeNav() {
    document.getElementById("myNav").style.width = "0%";
  }
 
  if (condition1) {
    //the first if/else will be executed if the data applies to the first condition//
  } else if (condition2) {
    // the second if/else will be executed if the data entered applies to the second condition//
  } else {
    // the third if/else statement will be executed if the statement applies to the third condtion//
  }